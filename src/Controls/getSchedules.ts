// query schedules collection for new or updated entries, return if there are.

import type { Firestore } from "firebase-admin/firestore"

export default async function getSchedules(db: Firestore) {}
